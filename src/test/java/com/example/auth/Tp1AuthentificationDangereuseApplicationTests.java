package com.example.auth;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Classe de test principale de l'application Spring Boot.
 *
 * Ce test permet de verifier que le contexte Spring
 * se charge correctement sans erreur.
 */
@SpringBootTest
class Tp1AuthentificationDangereuseApplicationTests {

	/**
	 * Test de chargement du contexte Spring.
	 * Si ce test passe, cela signifie que toute
	 * l'application demarre correctement.
	 */
	@Test
	void contextLoads() {
	}
}